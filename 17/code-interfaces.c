/* $Файл заголовков math.h определяет интерфейсы математических функций.$ */
#include <math.h>

/* $Функция sqrt определена в math.h декларацией$ */
/* $\textbf{double} sqrt(\textbf{double} X);$ */

int main() {
	double s = sqrt(9.0);
	/* $другой код$ */
}
